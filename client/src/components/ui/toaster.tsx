// Minimal toaster component for build compatibility
import React from 'react';

export const Toaster = () => {
  return null; // Empty component - just for build
};

export const toast = () => {
  // Empty function
};
